import { Button } from "@/components/ui/button";
export function AttestationControl({ disabled, pending = 0, onAttest }: { disabled?: boolean; pending?: number; onAttest: () => void }) { return <div className="flex items-center gap-3" data-testid="attestation-control"><Button type="button" disabled={disabled} onClick={onAttest} aria-describedby="attestation-status">I attest and apply approved changes</Button><span id="attestation-status" role="status" aria-live="polite" className="text-xs text-muted-foreground">{disabled ? "Attestation unavailable; no changes saved" : pending ? `${pending} approved change${pending === 1 ? "" : "s"} queued for sync` : "Exactly the reviewed changes will be saved"}</span></div>; }
