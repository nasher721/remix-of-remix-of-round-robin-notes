# Service Worker Stale Chunk Handling - Evidence

## Changes Made

### Change 1: Activate Handler (lines 74-82)
Added stale dynamic cache clearing on service worker activation.

```javascript
).then(async () => {
  // Clear stale dynamic cache entries on activation
  // This ensures old hashed chunks don't cause "Failed to fetch dynamically imported module" errors
  // after a deployment when index.html references new chunks
  const dynamicCache = await caches.open(DYNAMIC_CACHE);
  const dynamicKeys = await dynamicCache.keys();
  await Promise.all(dynamicKeys.map(key => dynamicCache.delete(key)));
  console.log('[SW] Cleared', dynamicKeys.length, 'stale dynamic cache entries');
  return self.clients.claim();
})
```

This runs BEFORE `self.clients.claim()` and clears ALL entries from DYNAMIC_CACHE.

### Change 2: Fetch Handler for JS Chunks (lines 110-112)
Added `networkFirstWithJsRetry` function (lines 151-169) that:
1. Tries network-first with cache fallback
2. On error, checks if it's a stale chunk error ("Failed to fetch" or "imported" in message)
3. If stale chunk: deletes the URL from dynamic cache, retries once
4. If retry fails: returns the error

```javascript
if (url.pathname.startsWith('/assets/') && /\.(js|mjs|css)$/i.test(url.pathname)) {
  event.respondWith(networkFirstWithJsRetry(request, DYNAMIC_CACHE, 24 * 60 * 60 * 1000));
  return;
}
```

## Verification

- Build: `npm run build` - SUCCESS (built in 11.31s)
- No changes to caching strategies (kept network-first for JS)
- No new cache names added
- Static asset caching unchanged

## Files Modified

- `public/sw.js` - Service worker with stale chunk handling
